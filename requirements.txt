# Python 3.11+ (getestet mit 3.13). Installation:
#   python -m venv .venv && .venv\Scripts\Activate.ps1 && pip install -r requirements.txt
#
# Wofür was gebraucht wird:
#   tests/test_pipeline.py   -> nur numpy. Deshalb installiert der CI-Workflow
#                               (.github/workflows/tests.yml) auch nur numpy.
#   backend.server/.collect  -> fastapi, uvicorn, websockets, numpy, torch
#   backend.train            -> zusätzlich scikit-learn (Accuracy, Konfusionsmatrix)
#   tests/smoke_e2e.py       -> zusätzlich httpx (für fastapi.testclient)
fastapi>=0.115
uvicorn[standard]>=0.34
websockets>=14
numpy>=2.1
torch>=2.6          # >=2.6 liefert Wheels für Python 3.13
scikit-learn>=1.5
httpx>=0.27         # nötig für fastapi.testclient (tests/smoke_e2e.py)
