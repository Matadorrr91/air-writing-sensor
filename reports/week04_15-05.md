# Week 04 Report — Machine Learning for Smart and Connected Systems (ML4SCS)

## Weekly Goal
Record real sensor data for digit "3", set up preprocessing pipeline on real data, and prepare for multi-class training.

## Work Done This Week

### 1. Data Work
Recorded 10 real sensor recordings of digit "3" using Phyphox on smartphone.
Data collected with Accelerometer (without gravity) and Gyroscope simultaneously.
Data structure: `Time (s), Acceleration x/y/z (m/s^2)` and `Time (s), Gyroscope x/y/z (rad/s)`.
Recordings exported as ZIP files, unpacked and organized into `real_data/digit_3_run01` to `digit_3_run10`.

Also conducted a signal processing experiment comparing Butterworth lowpass filter at different cutoff frequencies (1 Hz, 5 Hz, 15 Hz) on walking vs. jogging data, which confirmed that lower cutoff frequencies produce smoother signals and that jogging produces significantly larger acceleration amplitudes (±60 m/s²) compared to walking (±15 m/s²).

### 2. Analysis / Modeling Work
Updated `preprocessing.py` to work with real Phyphox data:
- Adjusted column names to match real CSV format (`Acceleration x (m/s^2)` instead of `Linear Acceleration`)
- Fixed Python 3.9 compatibility issue (`str | Path` syntax replaced)
- Successfully loads all 10 real recordings, generates feature matrix `(10, 24)`

Updated `train.py` to run on real data:
- Installed `scikit-learn` on local machine
- Random Forest trained on 10 real digit_3 recordings
- Cross-validation accuracy: 100% (expected for single class)

Implemented `predict.py`, a prediction function for new recordings:
- Loads a new recording, extracts features, runs model prediction
- Outputs predicted digit with probability distribution per class
- Model saved to disk using `joblib` for reuse

### 3. Repository / Documentation Work
- `preprocessing.py` updated for real data compatibility
- `train.py` updated with real data path
- `predict.py` added: prediction pipeline ready
- All scripts tested end-to-end on real data

## Experiments Conducted

| Experiment | Change Made | Result | Interpretation |
|-----------|-------------|--------|----------------|
| Butterworth filter comparison | Tested cutoff at 1, 5, 15 Hz on walking/jogging | Lower Hz = smoother signal | 1 Hz removes most noise but also movement detail |
| Real data preprocessing | Replaced dummy data with 10 real digit_3 recordings | Feature matrix (10, 24) generated | Pipeline works correctly on real sensor data |
| Real data training | Trained Random Forest on real recordings | 100% cross-val accuracy | Expected for single class |
| Prediction pipeline | Built predict.py with joblib model saving | Prediction + probability output working | Ready for multi-class testing |

## Results
Full pipeline working end-to-end on real data, from Phyphox recording to trained model to prediction output. Signal processing experiment confirmed Butterworth filter behaviour across different activity types.

## Challenges
- Data collection for digits 0, 1, 2 still pending; the presentation is next week
- Model cannot distinguish between digits yet and needs multi-class data urgently
- Python 3.9 compatibility issues required code adjustments

## Key Insights
- Real sensor data works well with the existing pipeline, no major adjustments needed
- Butterworth filter at 1 Hz is too aggressive for gesture recognition; 5 Hz is a better baseline
- Statistical features (mean, std, max, min) are sufficient as a baseline but may miss temporal patterns

## Plan for Next Week (Zwischenpräsentation: 21. Mai)
- Record 10 runs each for digits 0, 1, 2 today
- Retrain model on all 4 classes (0–3)
- Evaluate real multi-class accuracy
- Run live demo with predict.py for the presentation
- Prepare presentation slides

## Contributions
- Member 1: Isa Kilic
- Member 2: Efe Özüm
- Member 3: Tony Duong